# Projeto com FastAPI

Como utilizar:
 1. Faça um clone desse repositório
 2. Dentro do mesmo instale as Dependências 
    - pipx ()
    - poetry (Execute "poetry install" no terminal)
 3. Declare a versão do Python que vai utilizar (nesse projeto estou utilizando a mais atualizada "3.14.3")
